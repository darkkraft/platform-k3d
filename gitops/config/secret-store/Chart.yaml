apiVersion: v2
name: secret-store
description: ClusterSecretStore (Vault) + per-service ExternalSecrets materialising DB credentials from Vault KV.
type: application
version: 1.0.0
appVersion: "1.0.0"
