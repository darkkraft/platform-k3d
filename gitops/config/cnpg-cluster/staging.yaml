# CNPG cluster — staging overlay. Two instances (primary + 1 replica) so failover
# and streaming replication are validated before prod.
cluster:
  instances: 2
  storage:
    size: 5Gi
  resources:
    requests:
      cpu: 250m
      memory: 256Mi
    limits:
      cpu: "1"
      memory: 512Mi
